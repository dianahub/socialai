[build]
builder = "nixpacks"

[deploy]
startCommand = "npx prisma migrate resolve --applied 20260511185134_init 2>/dev/null; npx prisma migrate deploy && node server.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
